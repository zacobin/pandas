apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  creationTimestamp: null
  namespace: {{.Values.namespace.name}}
  labels:
    service: pandas-users-db-volume
  name: pandas-users-db-volume
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
status: {}
