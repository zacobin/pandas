apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  creationTimestamp: null
  namespace: {{.Values.namespace.name}}
  labels:
    service: pandas-es-redis-volume
  name: pandas-es-redis-volume
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
status: {}
